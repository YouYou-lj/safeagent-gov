{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only Tauri core window APIs are exposed; Sidecar process control remains Rust-only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
