# 非桌面研究复现工具

这里保存分布式队列、容器隔离和故障恢复实验所需的可选工具。GovSafeAgent 桌面端使用本地
Sidecar、SQLite 和进程内任务运行时，不依赖 Docker。

- `docker/`：历史服务端/分布式实验 Compose 与镜像定义。
- `scripts/`：容器和分布式恢复探针。
- `tests/`：仅验证研究部署安全边界的测试。

这些文件不应作为桌面安装或启动步骤。使用前必须显式指定本目录中的 Compose 文件。
