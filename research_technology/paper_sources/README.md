# 论文写作素材

- `docs/`：架构、威胁模型、核心机制、API 和工程治理文档。
- `plans/`：跨平台与历史开发规划，仅作为演进依据，不是当前运行说明。
- `project_history/`：阶段性发现与进度记录，用于追溯设计决策。

论文中的实现主张应优先引用 `../innovations/`、`../skills/`、`../mcp/` 和 `../benchmarks/`，
历史计划只能作为背景材料。
