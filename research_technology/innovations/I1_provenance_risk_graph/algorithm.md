# 机制设计

1. 适配用户、网页、文档、RAG、记忆五类来源，生成稳定 `source_id`、时间、信任域和内容哈希。
2. 将片段、指令、引用、检索和会话关系建图；节点保留规则、分类器和可选复核证据。
3. 在来源边和时序边上传播风险，限制单一低信任来源对高信任指令的覆盖能力。
4. 输出风险类别、置信度、证据子图、处置动作和版本信息，不只返回二值标签。
5. 以开发集校准阈值，在冻结留出集复算 Recall、Precision、FPR、ASR 和 P95 延迟。

当前实现仅覆盖第 2 步中的单片段规则证据，是后续对比基线。
